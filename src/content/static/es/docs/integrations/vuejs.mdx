---
section: integrations

order: 1

navigationTitle: "Vue"
navigationIconPath: '/images/brands/vuejs.svg'
image: '/integrations/vuejs/header.jpg'
ogImage: '/integrations/vuejs/og-image.jpg'

title: Uso de Stylify CSS en Vue.js
description: "Aprende a usar el CSS de Stylify con Vue.js. Codifica tu sitio web Vue.js más rápido con Stylify CSS"
howToSchemaTitle: 'Cómo usar Stylify CSS en Vue.js.'
howToSchemaSteps: [
	{
		"name": "Instalación",
		"text": "Instalar paquete @stylify/unplugin usando CLI como YARN o NPM",
		"url": "#instalación",
	},
	{
		"name": "Uso",
		"text": "Añade el módulo de construcción CSS de Stylify en vite.config.js",
		"url": "#usage",
	},
	{
		"name": "Configuración",
		"text": "Extiende la configuración CSS de Stylify como necesites. Configura variables, componentes, selectores personalizados y mucho más",
		"url": "#configuration",
	},
]
---
import StackBlitzLink from '@components/StackBlitzLink.astro';
import WhereToNext from '@components/WhereToNext.astro';

Vue.js es un framework progresivo de JavaScript.

<StackBlitzLink link="stylify-vue-vite" />

## Cómo integrar el CSS de Stylify con Vue.js y Vite.js

El ejemplo de abajo funciona con la plantilla Vite - Vue. Sin embargo, puedes utilizar el ejemplo de abajo y configurarlo para React.js, Vue y cualquier otro framework que utilices.

Primero, instala el paquete [@stylify/unplugin](/es/docs/unplugin) usando NPM o Yarn:

```
npm i -D @stylify/unplugin
yarn add -D @stylify/unplugin
```

A continuación añade la siguiente configuración en `vite.config.js`:

```javascript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { stylifyVite } from '@stylify/unplugin';

const stylifyPlugin = stylifyVite({
	bundles: [{ files: ['./src/**'], outputFile: './src/stylify.css' }],
	// Opcional
	// Información de configuración del compilador https://stylifycss.com/en/docs/stylify/compiler#configuration
	compiler: {
		// https://stylifycss.com/en/docs/stylify/compiler#variables
		variables: {},
		// https://stylifycss.com/en/docs/stylify/compiler#macros
		macros: {},
		// https://stylifycss.com/en/docs/stylify/compiler#components
		components: {},
		// ...
	}
});

export default defineConfig(({ mode}) => ({
	plugins: [stylifyPlugin, vue()]
}));
```

Ahora puedes añadir la ruta del `src/stylify.css` generado en el fichero `src/main.js`:

```javascript
import './stylify.css';
```

Ahora ejecuta `yarn dev`. Se generará el archivo `src/stylify.css`.

## Configuración
Hay muchas opciones que puedes configurar:
- [@stylify/unplugin](/es/docs/unplugin)
- [Compiler config](/es/docs/stylify/compiler)

<WhereToNext />
